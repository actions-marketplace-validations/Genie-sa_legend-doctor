# Pinned corpus audit — September 19, 2026

Analysis baseline: `378977306448c0fc3d0326dbc986f0f6e082b622`. The initial runner/CI commit changed no detector, pin, target, or gold label. A subsequent source-audited label correction is recorded below. All seven supplied working trees were clean at the listed commits. The findings below predate this branch and are not regressions introduced by the concurrent implementation tasks.

## Audit policy

Every row was checked against the pinned source, including the relevant local reads/writes and render slot. **Label drift** means a missing or outdated corpus expectation supported by that source review, not permission to copy predictions into labels. **Detector defect** means source establishes a violated invariant. **Unresolved** identifies a concrete proof still needed; it is neither a false-positive verdict nor an enforced label. Source-only audits do not establish native runtime cost. No bulk relabeling was performed.

## Pins

| Repository              | Commit                                     |
| ----------------------- | ------------------------------------------ |
| legend-music            | `59d02afc11d6b27bc53ddecf1a69501626f8487f` |
| excalidraw              | `abeeaeba217ab3b5193b78c8d8d63c373b518ced` |
| expensify               | `2f5f477478eef7d0f17b3dab14e5f4a35bbce411` |
| formbricks              | `b344ddd73cfb0f7af86573176d53534a5321d982` |
| outline                 | `bae9790355385b2b2987123eeb3978c6c06e6ed6` |
| open-webui-react-native | `7a277e6eac790db034204688bb9130d457545caa` |
| hoalu                   | `65a93432254c255a2be75a829b7316874a3aa22f` |

## Seven existing mismatches

| ID  | Pinned source                                                                                                                                                                     | Classification                       | Evidence and next action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | [DocumentCopy.tsx:27](https://github.com/outline/outline/blob/bae9790355385b2b2987123eeb3978c6c06e6ed6/app/components/DocumentExplorer/DocumentCopy.tsx#L27)                      | unresolved                           | Gold expects callback-timing-unresolved; actual is async-command-origin-unresolved. copy has default selectedPath, a duplicate await, and finally cleanup (43–66), and is called by both Button and DocumentExplorer. Explorer invokes onSubmit through submitNode and keyboard Enter (262,399). A source opportunity exists, but source alone does not choose the detector diagnostic taxonomy. Keep the mismatch until callback/default-argument proof is reviewed; do not change the gold reason merely to match output. |
| M2  | [PlaybackArea.tsx:29](https://github.com/LegendApp/legend-music/blob/59d02afc11d6b27bc53ddecf1a69501626f8487f/src/components/PlaybackArea.tsx#L29)                                | detector defect (missed opportunity) | isPlaying is used only for the stable Icon name (92). A module-scope wrapper around that slot can retain the subscription lifetime and remove playback-owner renders. Restore the structural single-leaf proof; retain the gold label.                                                                                                                                                                                                                                                                                      |
| M3  | [PlaybackArea.tsx:31](https://github.com/LegendApp/legend-music/blob/59d02afc11d6b27bc53ddecf1a69501626f8487f/src/components/PlaybackArea.tsx#L31)                                | detector defect (missed opportunity) | thumbnailVersion only supplies AlbumArt.reloadKey (84); the AlbumArt slot remains mounted. A wrapper can preserve ordinary currentTrack/size inputs and remove owner invalidations. Keep the gold label and investigate why the existing leaf proof abstains.                                                                                                                                                                                                                                                               |
| M4  | [VisualizerWindow.tsx:13](https://github.com/LegendApp/legend-music/blob/59d02afc11d6b27bc53ddecf1a69501626f8487f/src/visualizer/VisualizerWindow.tsx#L13)                        | detector defect (missed opportunity) | isPlaying only controls the stopped-overlay ternary (81–85). Extract a wrapper outside the ternary, keeping the wrapper always mounted and the inner View conditional. Keep the gold; fix conditional-slot proof rather than removing expected coverage.                                                                                                                                                                                                                                                                    |
| M5  | [expense-filter-dropdown.tsx:67](https://github.com/quanphm/hoalu/blob/65a93432254c255a2be75a829b7316874a3aa22f/apps/app/src/components/expenses/expense-filter-dropdown.tsx#L67) | unresolved                           | kindFilter only feeds CategoryFilterView inside the currentView category slot (263). Observable ownership stays outside the component. A wrapper remains stable in that original slot and gates its child; its surrounding PopoverContent/Portal can still close normally. This differs from U28, which moves state ownership. Actual inventory blocker is owner-commit-or-snapshot-work: audit memo dependencies and independent helper reads before extending the proof; the portal alone does not invalidate the label.  |
| M6  | [GeneralSettings.tsx:16](https://github.com/LegendApp/legend-music/blob/59d02afc11d6b27bc53ddecf1a69501626f8487f/src/settings/GeneralSettings.tsx#L16)                            | label drift (missing practice label) | globalHotkeyError only controls HotkeyCapture.className and the adjacent error Text (65–70) inside stable View 58. There are no event/effect reads. A single stable wrapper preserves all three uses and ordinary parent inputs while skipping the other settings sections. Add a separately reviewed practice label for this exact boundary.                                                                                                                                                                               |
| M7  | [VisualizerWindow.tsx:15](https://github.com/LegendApp/legend-music/blob/59d02afc11d6b27bc53ddecf1a69501626f8487f/src/visualizer/VisualizerWindow.tsx#L15)                        | label drift (missing practice label) | storedBinCount has only the pure nullish-default binCount derivation (16), used in PresetComponent.binCountOverride (79) and Select.value String conversion (117). Two stable wrappers cover every read; callback writes use the observable handle, not a captured value. Preserve the dynamic PresetComponent prop/remount behavior and add the missing multi-boundary label.                                                                                                                                              |

Three missed-opportunity detector gaps remain explicit. Two practice expectations can be added after a focused label review. The Outline reason and Hoalu owner-work proof remain unresolved. None was silently corrected or waived in the initial runner commit.

## All 31 unscored change findings

All are hook findings with disposition `change`. U28 is `move-state-down`; the others are `use-observable`. U28 and U29–U30 are cluster members and must be evaluated with their complete cluster. Target IDs resolve through the existing corpus manifests; links below point to the actual repository file, not the evaluator’s file-root-plus-basename display.

| ID  | Target / pinned hook                                                                                                                                                                                                                                                                               | Classification  | Source evidence                                                                                                                                                              | Action                                                                                                                                                                                                                                     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| U1  | `expensify-add-card-feed` / [isModalVisible:50](https://github.com/Expensify/App/blob/2f5f477478eef7d0f17b3dab14e5f4a35bbce411/src/pages/workspace/companyCards/addNew/DynamicAddNewCardPage.tsx#L50)                                                                                              | label drift     | The only value read is ConfirmModal.isVisible (153); Plaid exit and cancel/confirm only write it (133,159–162).                                                              | Add an independently reviewed missing hook label; retain the owner lifetime and confirmation command order.                                                                                                                                |
| U2  | `expensify-date-filter` / [value:16](https://github.com/Expensify/App/blob/2f5f477478eef7d0f17b3dab14e5f4a35bbce411/src/pages/Search/SearchAdvancedFiltersContentPage/DateFilterContentPageWrapper.tsx#L16)                                                                                        | unresolved      | DateFilterContent renders value (23), but the separate confirm Button reads it at command entry (38).                                                                        | Audit DateFilterContent.onChange/onDateModifierSelected together; require a command snapshot and no split transition when modifier changes.                                                                                                |
| U3  | `expensify-gps-permissions-flow` / [showLocationRequiredModal:52](https://github.com/Expensify/App/blob/2f5f477478eef7d0f17b3dab14e5f4a35bbce411/src/pages/iou/request/step/IOURequestStepDistanceGPS/GPSButtons/index.tsx#L52)                                                                    | unresolved      | PermissionsFlow.onDeny writes true (176); ConfirmModal reads isVisible (188), and confirm closes then opens OS settings (190–193).                                           | Trace the native permission callback scheduling and existing UI commit before enforcing a leaf migration.                                                                                                                                  |
| U4  | `expensify-gps-permissions-flow` / [showZeroDistanceModal:53](https://github.com/Expensify/App/blob/2f5f477478eef7d0f17b3dab14e5f4a35bbce411/src/pages/iou/request/step/IOURequestStepDistanceGPS/GPSButtons/index.tsx#L53)                                                                        | label drift     | saveGpsTrip writes true then returns only for zero distance (115–119); the single modal reads at 182 and acknowledges at 183.                                                | Add a missing label with the early-return command and other GPS controls as the render-cut evidence.                                                                                                                                       |
| U5  | `expensify-gps-permissions-flow` / [showDisabledServicesModal:54](https://github.com/Expensify/App/blob/2f5f477478eef7d0f17b3dab14e5f4a35bbce411/src/pages/iou/request/step/IOURequestStepDistanceGPS/GPSButtons/index.tsx#L54)                                                                    | unresolved      | Async service lookup writes the flag after await (67–71); modal confirm clears it before openSettings (124–126).                                                             | Preserve async completion and native settings timing; audit the imported command boundary before adding an enforced label.                                                                                                                 |
| U6  | `expensify-workspace-new-room-state` / [writeCapability:101](https://github.com/Expensify/App/blob/2f5f477478eef7d0f17b3dab14e5f4a35bbce411/src/pages/workspace/WorkspaceNewRoomPage.tsx#L101)                                                                                                     | unresolved      | The isAdminPolicy effect resets writeCapability (200–205); InputWrapper reads it (360) and room creation snapshots it (151).                                                 | Audit InputWrapper/ValuePicker callback forwarding and the form submit snapshot; preserve the effect and dependencies.                                                                                                                     |
| U7  | `formbricks-multiple-choice` / [isBulkEditOpen:59](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/editor/components/multiple-choice-element-form.tsx#L59)                                                                          | label drift     | Bulk-edit open/close writes are isolated (386,434); only BulkEditOptionsModal reads the flag (433), beside the main choice editor.                                           | Add a missing label for an owner-scoped observable and stable modal call-site wrapper; keep onSave and choice props in the owner.                                                                                                          |
| U8  | `formbricks-webhook-settings` / [openDeleteDialog:50](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/integrations/webhooks/components/webhook-settings-tab.tsx#L50)                                                                       | unresolved      | DeleteDialog alone reads open (333), but delete closes the outer settings tab before an awaited action (337–339).                                                            | Audit nested-dialog unmount and onDelete ordering before labeling; do not treat both open values as interchangeable.                                                                                                                       |
| U9  | `formbricks-single-tag` / [openDeleteTagDialog:46](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/workspaces/settings/tags/components/single-tag.tsx#L46)                                                                                 | label drift     | The delete trigger writes true (156); DeleteDialog consumes open and its boolean setter (160–161); tag editing siblings do not read it.                                      | Add a missing label after preserving the existing dialog close/confirm API; no state ownership move is required.                                                                                                                           |
| U10 | `formbricks-anonymous-links` / [numberOfLinks:43](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/app/%28app%29/workspaces/%5BworkspaceId%5D/surveys/%5BsurveyId%5D/%28analysis%29/summary/components/shareEmbedModal/anonymous-links-tab.tsx#L43) | label drift     | All numberOfLinks reads are input value, generate command, and disabled projection inside div 340 (347–355); the edit callback is local.                                     | Add a missing label retaining ownership above the conditional single-use section; move the local handler with the leaf.                                                                                                                    |
| U11 | `formbricks-survey-list` / [isAIDialogOpen:68](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/list/components/survey-list.tsx#L68)                                                                                                 | label drift     | NewSurveyMenu writes true from the AI item (112); CreateWithAIDialog is the sole value consumer (136–137).                                                                   | Add a missing label for the AI dialog wrapper, preserving the other template/route menu actions.                                                                                                                                           |
| U12 | `formbricks-survey-editor` / [invalidElements:92](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/editor/components/survey-editor.tsx#L92)                                                                                          | unresolved      | ElementsView alone renders invalidElements (216), while setters cross SurveyMenuBar and ElementsView boundaries (186,217).                                                   | Audit validation writers for updates coupled to active view, local survey, and publishing; do not infer independent atomicity from one reader.                                                                                             |
| U13 | `formbricks-survey-editor` / [hasIncompleteTranslations:93](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/editor/components/survey-editor.tsx#L93)                                                                                | unresolved      | SurveyEditorTabs alone renders the error flag (206); LanguagesView receives its setter (253).                                                                                | Trace language-validation effect writes and their coupling to survey/language updates before enforcing.                                                                                                                                    |
| U14 | `formbricks-survey-editor` / [isCautionDialogOpen:118](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/editor/components/survey-editor.tsx#L118)                                                                                    | unresolved      | EditPublicSurveyAlertDialog alone reads open (310); two other children receive its setter (194,223).                                                                         | Audit both publishing/edit callback chains and surrounding localSurvey transitions before enforcing.                                                                                                                                       |
| U15 | `formbricks-theme-styling` / [confirmResetStylingModalOpen:81](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/workspaces/settings/look/components/theme-styling.tsx#L81)                                                                  | label drift     | Reset button opens the flag (261); AlertDialog alone reads it (299), and confirm invokes onReset before closing (304–306).                                                   | Add a missing label preserving the reset command order and ordinary styling props; keep state lifetime in ThemeStyling.                                                                                                                    |
| U16 | `formbricks-create-segment` / [addFilterModalOpen:55](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/ee/contacts/segments/components/create-segment-modal.tsx#L55)                                                                        | unresolved      | AddFilterModal reads open/setOpen (233–235) under the outer segment dialog; its own content unmounts when closed.                                                            | Keep open ownership in CreateSegmentModal and audit nested-dialog callback/portal timing; never move the owner into dialog content.                                                                                                        |
| U17 | `formbricks-organization-actions` / [isInviteMemberModalOpen:72](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/organization/settings/teams/components/edit-memberships/organization-actions.tsx#L72)                                     | label drift     | Invite button sets true (187); InviteMemberModal is the only read (194–195), independent of leave-organization presentation.                                                 | Add a missing label preserving the imported modal callback API and owner lifetime.                                                                                                                                                         |
| U18 | `formbricks-profile-reset` / [showModal:60](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/app/%28app%29/workspaces/%5BworkspaceId%5D/settings/account/profile/components/EditProfileDetailsForm.tsx#L60)                                         | unresolved      | PasswordConfirmationModal reads showModal (287) alongside imperative form.getValues("email") (290); submit writes true (116).                                                | Determine whether isSubmitting/isDirty guarantees the same fresh email snapshot after removing the open-triggered parent render. Add a runtime case for repeated email edits; do not claim this is already a confirmed stale-value defect. |
| U19 | `formbricks-segment-settings` / [addFilterModalOpen:41](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/ee/contacts/segments/components/segment-settings.tsx#L41)                                                                          | unresolved      | AddFilterModal is the only open reader (230), under the segment settings view; onAddFilter mutates segment data.                                                             | Audit filter publication with dialog close and the imported portal lifetime before enforcing.                                                                                                                                              |
| U20 | `formbricks-when-to-send` / [isAddActionModalOpen:41](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/editor/components/when-to-send-card.tsx#L41)                                                                                  | label drift     | Only AddActionModal consumes this flag (365); the create trigger writes true (258) and its boolean setter is passed at 366.                                                  | Add a missing label; keep actionClasses/localSurvey updates and the separate edit-action modal unchanged in timing.                                                                                                                        |
| U21 | `formbricks-selected-row-settings` / [isDeleteDialogOpen:39](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/ui/components/data-table/components/selected-row-settings.tsx#L39)                                                            | unresolved      | DeleteDialog alone reads open (202), but deletion finally writes both isDeleting and open (97–98), and the table model supplies other dialog props.                          | Audit atomic completion with pending state and fresh table selection snapshots before labeling this independently.                                                                                                                         |
| U22 | `formbricks-custom-filter` / [hoveredRange:140](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/app/%28app%29/workspaces/%5BworkspaceId%5D/surveys/%5BsurveyId%5D/components/CustomFilter.tsx#L140)                                                | unresolved      | hoveredRange feeds only Calendar.selected (434), with hover writes (189,199) and mouse-leave reset (438).                                                                    | Audit calendar callback snapshot semantics and dateRange/selectingDate coupling; preserve the original hover-versus-click event order.                                                                                                     |
| U23 | `formbricks-create-dashboard` / [isCreating:28](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/ee/analysis/dashboards/components/create-dashboard-button.tsx#L28)                                                                         | label drift     | handleCreate writes true before await and false in finally (43–62); CreateDashboardDialog alone receives isCreating (78). Its form guards submit and buttons with that prop. | Add a missing label preserving the entire async command boundary and entry snapshot; only the dialog subscribes.                                                                                                                           |
| U24 | `formbricks-delete-contact` / [isDeletingPerson:26](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/ee/contacts/%5BcontactId%5D/components/delete-contact-button.tsx#L26)                                                                  | unresolved      | DeleteDialog reads isDeletingPerson (68); finally clears both pending and deleteDialogOpen (44–45).                                                                          | Audit one atomic completion across both states and navigation/unmount; a single-leaf read alone does not prove publication timing.                                                                                                         |
| U25 | `formbricks-taxonomy-tree` / [value:232](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/ee/unify-feedback/topics-subtopics/components/taxonomy-display/taxonomy-tree.tsx#L232)                                                            | unresolved      | value.trim() feeds Input, save-button disabled, and async save (232–242,265–277); the separate cancel button depends only on isSaving.                                       | Verify one current-value command snapshot shared by Enter/click, sibling publication, and a material owner render cut before enforcing.                                                                                                    |
| U26 | `formbricks-survey-menu-bar` / [isConfirmDialogOpen:72](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/editor/components/survey-menu-bar.tsx#L72)                                                                                  | unresolved      | AlertDialog alone reads open (689); attempted navigation sets it (179), and confirm clears it before save work (696 onward).                                                 | Audit event/saving/navigation callback order and captures; preserve unsaved-change confirmation behavior.                                                                                                                                  |
| U27 | `formbricks-delete-workspace` / [isDeleteDialogOpen:34](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/workspaces/settings/general/components/delete-workspace-render.tsx#L34)                                                            | unresolved      | DeleteDialog alone reads open (120); handleDeleteDialogOpenChange also clears confirmationName before setting open (42–46).                                                  | Audit the close/reset atomic transition and pending completion before splitting the open subscription.                                                                                                                                     |
| U28 | `formbricks-add-filter-modal` / [searchValue:180](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/ee/contacts/segments/components/add-filter-modal.tsx#L180)                                                                               | detector defect | searchValue is grouped with activeTabId and moved into div 549 inside DialogContent (543). Owner state currently outlives Radix conditional content.                         | Retain state ownership above DialogContent or prove the complete ancestor mount chain. See reproduction below; this is pre-existing at the audited SHA.                                                                                    |
| U29 | `formbricks-response-options-card` / [recaptchaToggle:54](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/editor/components/response-options-card.tsx#L54)                                                                          | unresolved      | recaptchaToggle reads/writes are local to AdvancedOptionToggle 442, but the same command also writes recaptchaThreshold and parent localSurvey (281–291).                    | Audit atomic local-model and parent-survey publication plus whether the parent update already rerenders this owner; no proven net render saving yet.                                                                                       |
| U30 | `formbricks-response-options-card` / [recaptchaThreshold:65](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/editor/components/response-options-card.tsx#L65)                                                                       | unresolved      | recaptchaThreshold renders label/Slider (451,463), is paired with toggle reset (286), and updates parent localSurvey on every slider change (295–303).                       | Same cluster as the preceding row: prove cross-owner atomicity and a saved render rather than adding two independent labels.                                                                                                               |
| U31 | `formbricks-response-options-card` / [verifyProtectWithPinError:87](https://github.com/formbricks/formbricks/blob/b344ddd73cfb0f7af86573176d53534a5321d982/apps/web/modules/survey/editor/components/response-options-card.tsx#L87)                                                                | label drift     | The PIN-error flag is only read within div 545 (552,562–563); blur validation only writes the error (101–109).                                                               | Add a missing label retaining ownership above conditional PIN UI, moving the local validation callback and passing current survey/t translation inputs.                                                                                    |

Audit disposition of these 31 rows: 11 label drift, 19 unresolved, 1 detector defect. These remain unscored by the current hook precision metric; the ledger is not loaded as gold data.

## U28: ownership lost under conditional dialog content

The pin uses `@radix-ui/react-dialog` **1.1.15** (`pnpm-lock.yaml:316–318`). `AddFilterModal` owns `activeTabId` and `searchValue` at lines 179–180. Its callers keep the component rendered and toggle its `open` prop: `create-segment-modal.tsx:228–235` and `segment-settings.tsx:223–232`. Inside it, the proposed extracted div at 549 is under `DialogContent` at 543. The local UI wrapper (`modules/ui/components/dialog/index.tsx:70–90`) renders `DialogPortal` and `DialogPrimitive.Content` without `forceMount`. The [published pinned Radix implementation](https://unpkg.com/@radix-ui/react-dialog@1.1.15/dist/index.mjs) gates Portal and Content with Presence on `forceMount || context.open`. Closing the dialog therefore unmounts the proposed new owner while the existing owner remains mounted.

Minimal reproduction sketch (independent of application names):

```tsx
function DialogOwner({ open }) {
  const [query, setQuery] = useState("");
  return (
    <ConditionalContent open={open}>
      <FilterBody query={query} onChange={setQuery} />
    </ConditionalContent>
  );
}
// ConditionalContent returns open ? children : null.
// Moving useState into FilterBody changes its lifetime.
```

Keep DialogOwner mounted; open, type a query/change tab, close until content unmounts, then reopen. Original owner retains query/tab; the recommended moved owner reinitializes them. This is a source-proven defect with a reproduction sketch, not a claimed executed Radix runtime test. Follow-up: add a custom conditional-container adversarial fixture and a pinned negative label before changing the detector. Prove ancestor mount identity structurally or retain ownership above it; do not add a Radix/component-name allowlist.

## Validation and runner risk ledger

| Risk | Contract / plausible defect                                       | Evidence                                                                                                                                                                               |
| ---- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1   | Unknown names or ignored flags produce successful empty metrics   | CLI regression failed on original runner; unknown/malformed/duplicate assignments and unknown flags now fail.                                                                          |
| R2   | Partial results masquerade as complete coverage                   | CLI test checks every omission and complete-mode early failure; selection tests include the loaded private slice and default/explicit partial mode.                                    |
| R3   | Off-pin inputs leave zero targets but still print perfect metrics | Real temporary Git checkout test verifies off-pin error plus zero-target failure and absence of percentages; it failed before the fix.                                                 |
| R4   | Valid paths containing spaces or equals are corrupted             | Selection test checks exact resolved path and successful complete selection.                                                                                                           |
| R5   | CI pin list or arguments drift from scored manifests              | Both workflow scripts import the built public repository manifest; actual fetch/evaluate commands are validated locally. Hosted Actions cache service itself is not exercised locally. |

- `npm run check`: passed lint, formatting, typecheck, build, **987 unit/runtime tests**, and package dry run.
- Actual `--partial` Open WebUI run: exit 0, 5 hooks/3 targets, mode and all six omissions printed.
- Actual `--complete` seven-app run: exit 1, **1,236 hooks / 237 targets**, exactly the seven baseline failures above; no new failure. Hook labels 508/523, known non-enforced misses 15, practice labels 67/71, grouped labels 10/10, unscored changes 31/248.
- Before/after `src --coverage` Doctor scans passed with zero hooks/findings/practices (smoke check, not optimization proof).
- CLI tests were red on the original unknown-name, omitted-repository, and empty-metrics paths. A temporary compiled-test mutation back to `URL.pathname` failed the spaced/hash/non-ASCII path regression; the original compiled output was restored.
- Expanded cases cover missing option values, empty names/paths, wrong-case and unknown names, duplicate assignments, conflicting modes in both orders, missing checkout errors, complete runs with all paths supplied but all pins wrong, private-slice completeness, repeated same mode, argument ordering, and caller-array preservation.
- The exact workflow fetch script completed all seven remote pins into disposable clean checkouts without installing application dependencies. The hosted Actions cache service is not reproduced locally.

No new detector timing/alias/effect transitions are introduced by this runner change. The audit flags those questions where they already occur in application migrations; the runner tests do not claim to prove them. Tests establish the enumerated cases, not every possible CLI input or external Git/network failure.

## Per-application deltas for the runner change

Full before/after target reports are identical, including every hook and practice action × disposition combination. Counts below are hook change/candidate/keep and practice change/style. Every action delta and every disposition delta is **0**, including applications with no practices.

| Application             | Hooks / targets | Hook change / candidate / keep | Practice change / style | Action delta | Disposition delta |
| ----------------------- | --------------- | ------------------------------ | ----------------------- | ------------ | ----------------- |
| legend-music            | 51 / 1          | 14 / 18 / 19                   | 49 / 2                  | 0            | 0                 |
| excalidraw              | 186 / 2         | 8 / 93 / 85                    | 0 / 0                   | 0            | 0                 |
| expensify               | 345 / 95        | 66 / 188 / 91                  | 0 / 0                   | 0            | 0                 |
| formbricks              | 422 / 90        | 127 / 253 / 42                 | 0 / 0                   | 0            | 0                 |
| outline                 | 150 / 45        | 22 / 80 / 48                   | 0 / 0                   | 0            | 0                 |
| open-webui-react-native | 5 / 3           | 3 / 1 / 1                      | 3 / 0                   | 0            | 0                 |
| hoalu                   | 77 / 1          | 8 / 55 / 14                    | 15 / 0                  | 0            | 0                 |

## Source-audited follow-up to the seven mismatches

Two missing practice labels are now added, each with an explicit `change` disposition and a source-based rationale:

- **M6, GeneralSettings:16:** the error string has exactly three render reads, all within stable View 58. Its callback/effect use set is empty. The other two subscriptions remain ordinary parent inputs; a single extracted boundary covers the error border and Text together. The other settings sections do not consume it.
- **M7, VisualizerWindow:15:** `VisualizerPreferences.visualizer.binCount` is numeric (`visualizer/preferences.ts:10`). Its only derived binding is `storedBinCount ?? 64`; every consumer is in the two stable call sites at 79 and 115. The dynamic preset constructor stays a parent prop, so switching presets retains its existing remount behavior. `handleBinCountChange` writes through the observable handle and never consumes the captured value.

These additions remove only the two unexpected-practice failures. They do not change predictions, delete expectations, or disable enforcement. On fresh downloaded pins, the full run now exits **1 with five failures**, practice labels **69/73**, practice precision **69/69**, hooks **508/523**, and the same **31 unscored changes**. Precision changed because two previously unscored predictions received independent source review, not because a detector improved. All seven applications still have **zero action/disposition deltas**, as tabulated above. `npm run check` still passes **987 tests**; pre/post Legend Music Doctor reports have identical findings, practices, subscriptions, and hook inventory.

The remaining five failures are blockers to merging the new gate:

| Failure                           | Concrete proof still needed                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1: Outline copying reason        | The async command has a default parameter and multiple imported callback entry paths. Preserve the known non-enforced `use-observable` opportunity; decide the exact diagnostic through structural command-origin/callback analysis, not by rewriting the expected enum to current output.                                                                                                                                                                                  |
| M2–M3: PlaybackArea subscriptions | `ownerHasMutableRenderRead` rejects independent `cn(...)` calls in JSX. The pinned helper delegates to imported `clsx` and `twMerge`. A closed dependency/purity proof is needed before skipping those owner evaluations; a component/helper-name exception is not valid evidence.                                                                                                                                                                                          |
| M4: VisualizerWindow playback     | The independently subscribed bin-count flow contains `String(binCount)`. `trackedCalls` validates pure expressions only against the subscription currently being moved, so this independent conversion remains an unproven imperative read. Extend primitive-flow proof with shadowing/coercion/alias controls before exempting it.                                                                                                                                         |
| M5: Hoalu kind filter             | Inventory reports `owner-commit-or-snapshot-work`. Memo dependencies include derived `dateRange`, imported live-query arrays, navigation functions, and setters; current stable-dependency proof handles only primitive constants/props or direct subscriptions. Prove the relevant independent memo and callback work without relaxing arbitrary object/function dependencies. Observable ownership stays external; the existing portal is not itself an ownership defect. |

Small executed analyzer probes isolated these guards (same baseline detector, no detector changes):

| Probe, with one narrow `open` leaf and twelve unrelated JSX elements | Result                                                             |
| -------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Literal independent parent prop                                      | `open` relocation is emitted.                                      |
| Independent call to a local pure identity helper                     | No relocation; helper summaries are not established by this guard. |
| Independent unproven helper call                                     | No relocation, the necessary negative control.                     |
| Another direct primitive subscription rendered without conversion    | Both subscriptions can relocate.                                   |
| That primitive subscription rendered through `String(raw ?? 64)`     | Only that primitive flow relocates; `open` is blocked.             |
| Memo depending on an independently computed `readRange()` object     | No relocation; dependency stability is not proven.                 |

The probe sources and outputs accompany the PR evidence artifacts. These are reproducible proof-gap probes, not new enforced rules or claims that arbitrary helper calls are safe. No detector implementation was changed merely to make the gate pass.

## Follow-up source audit and detector repair

The earlier five-blocker assessment above records the initial investigation. Further source review resolves two
incorrect expectations without broadening a detector:

- **M1:** `copying` is written by the async command but never read in a deferred callback. Its render reads are the
  Button label and disabled state. The default argument captures `selectedPath`, not `copying`. The corrected gold
  reason is `async-command-origin-unresolved`; the desired action stays non-enforced. The
  [source audit and diagnostic regression evidence](research/outline-async-origin.md) distinguish this from actual
  captured pending-state reads.
- **M5:** a category-only subscription cut can skip the mounted date panel's independent render-clock refresh.
  `DateFilterView` reads `new Date()` at pinned line 717 and sets `Calendar.endMonth` from its year at 743.
  The normal/StrictMode runtime counterexample advances the clock across New Year and updates only category kind:
  the original calendar advances its end year, while the proposed relocation leaves it stale without remounting.
  The old positive label was invalid and is removed. The explicit
  [non-enforced research label](research/hoalu-filter-render-clock.json) preserves the counterexample; any future
  emitted actionable practice at this location still fails ordinary unlabeled-practice precision. The existing
  owner-work guard remains intact.

**M2–M4** require actual detector changes, not label edits: source-proven class wrappers must also have safe argument
evaluation, and independent numeric conversions must retain their lexical binding proof. The pinned positive
labels remain enforced. Their rationales now identify these exact proof obligations.

The follow-up also adds five Changesets covering PRs 17–21. Changesets plans the combined next release as **0.5.0**;
this does not publish a release or merge a version PR. Final combined checks and all-application deltas are recorded
in [the PR evidence](pr-17-evidence.md).
